var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { useQuery } from '@tanstack/react-query';
import { useDataProvider } from 'ra-core';
export var useAPISchema = function (_a) {
    var _b = _a === void 0 ? {} : _a, options = _b.options;
    var dataProvider = useDataProvider();
    if (!dataProvider.getSchema) {
        throw new Error("The data provider doesn't have access to the database schema");
    }
    return useQuery(__assign({ queryKey: ['getSchema'], queryFn: function () { return dataProvider.getSchema(); }, staleTime: 1000 * 60 }, options));
};
