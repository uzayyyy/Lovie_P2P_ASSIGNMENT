import { OnError, OnSuccess } from 'ra-core';
import { UseMutationResult } from '@tanstack/react-query';
import { ResetPasswordParams } from './authProvider';
/**
 * This hook returns a function to call in order to reset a user password on Supabase.
 *
 * @example
 * import { useSupabaseAccessToken } from 'ra-supabase-core';
 *
 * const ResetPasswordPage = () => {
 *     const [resetPassword] = useResetPassword();
 *
 *     const handleSubmit = event => {
 *         resetPassword({
 *             email: event.currentTarget.elements.email.value,
 *         });
 *     };
 *
 *     return (
 *         <form onSubmit={handleSubmit}>
 *             <label for="email">Email:</label>
 *             <input id="email" name="email" type="email" />
 *             <button type="submit">Reset password</button>
 *         </form>
 *     );
 * };
 **/
export declare const useResetPassword: (options?: UseResetPasswordOptions) => [UseMutationResult<unknown, Error, ResetPasswordParams>["mutate"], UseMutationResult<unknown, Error, ResetPasswordParams>];
export type UseResetPasswordOptions = {
    onSuccess?: OnSuccess;
    onError?: OnError;
};
//# sourceMappingURL=useResetPassword.d.ts.map