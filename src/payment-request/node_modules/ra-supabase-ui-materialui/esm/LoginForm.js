var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import * as React from 'react';
import { Box, styled } from '@mui/material';
import { required, useTranslate } from 'ra-core';
import { Link, LoginForm as RaLoginForm, PasswordInput, TextInput, } from 'ra-ui-materialui';
import { ForgotPasswordPage } from './ForgotPasswordPage';
/**
 * A component that renders a form to login to the application with an email and password.
 */
export var LoginForm = function (_a) {
    var disableForgotPassword = _a.disableForgotPassword, props = __rest(_a, ["disableForgotPassword"]);
    var translate = useTranslate();
    return (React.createElement(Root, null,
        React.createElement(RaLoginForm, __assign({}, props),
            React.createElement(TextInput, { autoFocus: true, source: "email", type: "email", label: translate('ra.auth.email', {
                    _: 'Email',
                }), autoComplete: "email", validate: required() }),
            React.createElement(PasswordInput, { source: "password", label: translate('ra.auth.password', {
                    _: 'Password',
                }), autoComplete: "current-password", validate: required() })),
        !disableForgotPassword ? (React.createElement(Box, { sx: {
                textAlign: 'center',
                mb: 1,
            } },
            React.createElement(Link, { to: ForgotPasswordPage.path, variant: "body2" }, translate('ra-supabase.auth.forgot_password', {
                _: 'Forgot password?',
            })))) : null));
};
var PREFIX = 'RaSupabaseLoginForm';
var Root = styled('div', {
    name: PREFIX,
    overridesResolver: function (props, styles) { return styles.root; },
})(function () { return ({}); });
