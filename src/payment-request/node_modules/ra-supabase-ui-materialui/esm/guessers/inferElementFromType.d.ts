import { type InferredTypeMap } from 'ra-core';
import type { OpenAPIV2 } from 'openapi-types';
import { InferredElement } from './InferredElement';
export declare const inferElementFromType: ({ name, description, format, type, requiredFields, types, props, schema, }: {
    name: string;
    types: InferredTypeMap;
    description?: string;
    format?: string;
    type?: string;
    requiredFields?: string[];
    props?: any;
    schema: OpenAPIV2.Document;
}) => InferredElement;
//# sourceMappingURL=inferElementFromType.d.ts.map