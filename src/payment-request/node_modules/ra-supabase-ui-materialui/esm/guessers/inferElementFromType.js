var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import { required } from 'ra-core';
import { pluralize } from 'inflection';
import { InferredElement } from './InferredElement';
var hasType = function (type, types) { return typeof types[type] !== 'undefined'; };
export var inferElementFromType = function (_a) {
    var _b, _c;
    var name = _a.name, description = _a.description, format = _a.format, type = _a.type, requiredFields = _a.requiredFields, types = _a.types, props = _a.props, schema = _a.schema;
    if (name === 'id' && hasType('id', types)) {
        return new InferredElement(types.id, { source: 'id' });
    }
    var validate = (requiredFields === null || requiredFields === void 0 ? void 0 : requiredFields.includes(name)) ? [required()] : undefined;
    if ((description === null || description === void 0 ? void 0 : description.startsWith('Note:\nThis is a Foreign Key to')) &&
        hasType('reference', types)) {
        var reference = description.split('`')[1].split('.')[0];
        var referenceResourceDefinition = (_b = schema.definitions) === null || _b === void 0 ? void 0 : _b[reference];
        if (!referenceResourceDefinition) {
            throw new Error("The referenced resource ".concat(reference, " is not defined in the API schema"));
        }
        var referenceRecordRepresentationField = inferRecordRepresentationField(referenceResourceDefinition);
        return new InferredElement(types.reference, __assign({ source: name, reference: reference }, props), hasType('autocompleteInput', types) &&
            referenceRecordRepresentationField
            ? [
                new InferredElement(types.autocompleteInput, {
                    optionText: referenceRecordRepresentationField,
                }),
            ]
            : undefined, hasType('autocompleteInput', types) &&
            !referenceRecordRepresentationField
            ? "Could not infer the field to use to filter referenced ".concat(reference, " records. Please provide the `filterToQuery` prop to the <AutocompleteInput> component. See https://github.com/marmelab/ra-supabase/blob/main/packages/ra-supabase/README.md#autocompleteinput-with-references")
            : undefined);
    }
    if (name.substring(name.length - 4) === '_ids' &&
        hasType('referenceArray', types)) {
        var reference = pluralize(name.substr(0, name.length - 4));
        var referenceResourceDefinition = (_c = schema.definitions) === null || _c === void 0 ? void 0 : _c[reference];
        if (!referenceResourceDefinition) {
            throw new Error("The referenced resource ".concat(reference, " is not defined in the API schema"));
        }
        var referenceRecordRepresentationField = inferRecordRepresentationField(referenceResourceDefinition);
        return new InferredElement(types.referenceArray, __assign({ source: name, reference: reference }, props), hasType('autocompleteArrayInput', types) &&
            referenceRecordRepresentationField
            ? [
                new InferredElement(types.autocompleteArrayInput, {
                    optionText: referenceRecordRepresentationField,
                }),
            ]
            : undefined, hasType('autocompleteArrayInput', types) &&
            !referenceRecordRepresentationField
            ? "Could not infer the field to use to filter referenced ".concat(reference, " records. Please provide the `filterToQuery` prop to the <AutocompleteArrayInput> component. See https://github.com/marmelab/ra-supabase/blob/main/packages/ra-supabase/README.md#autocompleteinput-with-references")
            : undefined);
    }
    if (type === 'array') {
        // FIXME instrospect further
        return new InferredElement(types.string, {
            source: name,
            validate: validate,
        });
    }
    if (type === 'string') {
        if (name === 'email' && hasType('email', types)) {
            return new InferredElement(types.email, __assign({ source: name, validate: validate }, props));
        }
        if (['url', 'website'].includes(name) && hasType('url', types)) {
            return new InferredElement(types.url, __assign({ source: name, validate: validate }, props));
        }
        if (format &&
            [
                'timestamp with time zone',
                'timestamp without time zone',
            ].includes(format) &&
            hasType('date', types)) {
            return new InferredElement(types.date, __assign({ source: name, validate: validate }, props));
        }
    }
    if (type === 'integer' && hasType('number', types)) {
        return new InferredElement(types.number, __assign({ source: name, validate: validate }, props));
    }
    if (type && hasType(type, types)) {
        return new InferredElement(types[type], __assign({ source: name, validate: validate }, props));
    }
    return new InferredElement(types.string, __assign({ source: name, validate: validate }, props));
};
var inferRecordRepresentationField = function (referenceResourceDefinition) {
    var _a, _b, _c, _d;
    if (((_a = referenceResourceDefinition.properties) === null || _a === void 0 ? void 0 : _a.name) != null) {
        return 'name';
    }
    if (((_b = referenceResourceDefinition.properties) === null || _b === void 0 ? void 0 : _b.title) != null) {
        return 'title';
    }
    if (((_c = referenceResourceDefinition.properties) === null || _c === void 0 ? void 0 : _c.label) != null) {
        return 'label';
    }
    if (((_d = referenceResourceDefinition.properties) === null || _d === void 0 ? void 0 : _d.reference) != null) {
        return 'reference';
    }
};
