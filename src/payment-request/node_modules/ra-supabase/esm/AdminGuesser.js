var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import * as React from 'react';
import { AdminContext, AdminUI, Resource, Loading, CustomRoutes, } from 'react-admin';
import { Route, BrowserRouter } from 'react-router-dom';
import { supabaseDataProvider, supabaseAuthProvider } from 'ra-supabase-core';
import { useCrudGuesser, LoginPage, SetPasswordPage, ForgotPasswordPage, } from 'ra-supabase-ui-materialui';
import { createClient } from '@supabase/supabase-js';
import { defaultI18nProvider } from './defaultI18nProvider';
export var AdminGuesser = function (props) {
    var instanceUrl = props.instanceUrl, apiKey = props.apiKey, dataProvider = props.dataProvider, authProvider = props.authProvider, basename = props.basename, darkTheme = props.darkTheme, defaultTheme = props.defaultTheme, _a = props.i18nProvider, i18nProvider = _a === void 0 ? defaultI18nProvider : _a, lightTheme = props.lightTheme, queryClient = props.queryClient, store = props.store, theme = props.theme, rest = __rest(props, ["instanceUrl", "apiKey", "dataProvider", "authProvider", "basename", "darkTheme", "defaultTheme", "i18nProvider", "lightTheme", "queryClient", "store", "theme"]);
    var defaultSupabaseClient = instanceUrl && apiKey ? createClient(instanceUrl, apiKey) : null;
    var defaultDataProvider = instanceUrl && apiKey && defaultSupabaseClient
        ? supabaseDataProvider({
            instanceUrl: instanceUrl,
            apiKey: apiKey,
            supabaseClient: defaultSupabaseClient,
        })
        : undefined;
    var defaultAuthProvider = instanceUrl && apiKey && defaultSupabaseClient
        ? supabaseAuthProvider(defaultSupabaseClient, {})
        : undefined;
    return (React.createElement(BrowserRouter, null,
        React.createElement(AdminContext, { authProvider: authProvider !== null && authProvider !== void 0 ? authProvider : defaultAuthProvider, basename: basename, darkTheme: darkTheme, dataProvider: dataProvider !== null && dataProvider !== void 0 ? dataProvider : defaultDataProvider, defaultTheme: defaultTheme, i18nProvider: i18nProvider, lightTheme: lightTheme, queryClient: queryClient, store: store, theme: theme },
            React.createElement(AdminUIGuesser, __assign({}, rest)))));
};
var AdminUIGuesser = function (props) {
    var resourceDefinitions = useCrudGuesser();
    var children = props.children, rest = __rest(props, ["children"]);
    // while we're guessing, we don't want to show the not found page
    var _a = React.useState(function () { return Loading; }), CatchAll = _a[0], setCatchAll = _a[1];
    React.useEffect(function () {
        if (!children && resourceDefinitions.length > 0) {
            console.log("Guessed Admin:\n\nimport { Admin, Resource, CustomRoutes } from 'react-admin';\nimport { BrowserRouter, Route } from 'react-router-dom';\nimport { createClient } from '@supabase/supabase-js';\nimport {\n    CreateGuesser,\n    EditGuesser,\n    ForgotPasswordPage,\n    ListGuesser,\n    LoginPage,\n    SetPasswordPage,\n    ShowGuesser,\n    defaultI18nProvider,\n    supabaseDataProvider,\n    supabaseAuthProvider\n} from 'ra-supabase';   \n\nconst instanceUrl = YOUR_SUPABASE_URL;\nconst apiKey = YOUR_SUPABASE_API_KEY;\nconst supabaseClient = createClient(instanceUrl, apiKey);\nconst dataProvider = supabaseDataProvider({ instanceUrl, apiKey, supabaseClient });\nconst authProvider = supabaseAuthProvider(supabaseClient, {});\n\nexport const App = () => (\n    <BrowserRouter>\n        <Admin\n            dataProvider={dataProvider}\n            authProvider={authProvider}\n            i18nProvider={defaultI18nProvider}\n            loginPage={LoginPage}\n        >".concat(resourceDefinitions
                .map(function (def) { return "\n            <Resource name=\"".concat(def.name, "\"").concat(def.list ? ' list={ListGuesser}' : '').concat(def.edit ? ' edit={EditGuesser}' : '').concat(def.create ? ' create={CreateGuesser}' : '').concat(def.show ? ' show={ShowGuesser}' : '', " />"); })
                .join(''), "\n            <CustomRoutes noLayout>\n                <Route path={SetPasswordPage.path} element={<SetPasswordPage />} />\n                <Route path={ForgotPasswordPage.path} element={<ForgotPasswordPage />} />\n            </CustomRoutes>\n        </Admin>\n    </BrowserRouter>\n);"));
        }
    }, [resourceDefinitions, children]);
    React.useEffect(function () {
        // once we have guessed all the resources, we can show the not found page for unknown paths
        if (!children && resourceDefinitions.length > 0) {
            setCatchAll(undefined);
        }
    }, [resourceDefinitions, children]);
    var resourceElements = resourceDefinitions.map(function (resourceDefinition) { return (React.createElement(Resource, __assign({ key: resourceDefinition.name }, resourceDefinition))); });
    return (React.createElement(AdminUI, __assign({ ready: Loading, catchAll: CatchAll, loginPage: LoginPage }, rest), children !== null && children !== void 0 ? children : resourceElements,
        React.createElement(CustomRoutes, { noLayout: true },
            React.createElement(Route, { path: SetPasswordPage.path, element: React.createElement(SetPasswordPage, null) }),
            React.createElement(Route, { path: ForgotPasswordPage.path, element: React.createElement(ForgotPasswordPage, null) }))));
};
